Finding files

find

big files
old files

basic syntax

find /path/to/dir -name "filename"

find files larger than 50k

find /path/to/dir -type f -size +50k -exec du -sh {} \;

alternativaley use du

find files older than 7 days

find /path/to/dir +mtime 7


which

executable

locate

quick based on database
